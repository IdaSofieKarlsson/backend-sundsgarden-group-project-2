function LoginPage() {
    return <div>
        <h1>Login page!</h1>
        <p>This is the login page.</p>
        <p>This can be the default start page, with a button that leads to the register page.</p>
    </div>
}

export default LoginPage;